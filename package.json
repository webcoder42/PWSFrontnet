{
  "name": "mypsw-merged-workspace",
  "private": true,
  "workspaces": [
    "admin",
    "client",
    "PWS"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=psw-plus",
    "build": "npm run build --workspace=psw-plus",
    "preview": "npm run preview --workspace=psw-plus",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
