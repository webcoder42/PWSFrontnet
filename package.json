{
  "name": "mypsw-merged-workspace",
  "private": true,
  "workspaces": [
    "admin",
    "client",
    "PWS"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=psw-plus",
    "dev:all": "node run-all.js",
    "dev:admin": "npm run dev --workspace=admin-dashboard-psw-plus",
    "dev:client": "npm run dev --workspace=patient",
    "dev:psw": "npm run dev --workspace=psw-plus",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}